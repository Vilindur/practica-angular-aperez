import { PuntoFinalPipe } from './punto-final.pipe';

describe('PuntoFinalPipe', () => {
  it('create an instance', () => {
    const pipe = new PuntoFinalPipe();
    expect(pipe).toBeTruthy();
  });
});
